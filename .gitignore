# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Test coverage (generated by jest --coverage)
coverage/

# Windows reserved-name accident (e.g. bad shell redirect)
NUL

# Env files with secrets (never commit)
.env
.env.production
.env.local
.env.development.local
.env.test.local
.env.production.local

# Keep the template (safe placeholders only)
!.env.local.example

# Debug / OS
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/extensions.json

# TypeScript (if you add TS later)
*.tsbuildinfo

# Vercel
.vercel
